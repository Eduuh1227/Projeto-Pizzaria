# Production requires a persistent PostgreSQL database (for example, Neon).
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# Exact public origin, no trailing path. Requests from other origins are denied.
APP_ORIGIN=https://your-domain.example
# Local development uses .data/realeza.sqlite when DATABASE_URL is absent.
